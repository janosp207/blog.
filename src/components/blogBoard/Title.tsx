import { Box, Typography } from "@mui/material";

const Title = () => {
	return (
		<Box textAlign='center' mt={5} mb={2}>
			<Typography variant="h6" fontWeight='bold'>Latest Blog. posts</Typography>
		</Box>
	)
}

export default Title